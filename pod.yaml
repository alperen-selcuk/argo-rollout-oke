apiVersion: v1
kind: Pod
metadata:
  name: python-app
  labels:
    app: python
spec:
  containers:
    - name: python-app
      image: iad.ocir.io/natdcshjumpstartprod/argo:v1
      env:
        - name: COLOR
          value: "RED"
